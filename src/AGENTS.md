# src

## Purpose

Library source shipped (via Vite lib build) as `vue-relative-rotation-chart`.

## Belongs here

- `components/`, `composables/`, `types/`, `utils/`, `scenarios/`, package `index.ts`

## Does not belong here

- Demo app entry (`demo/`)
- Vitest files (`tests/`)
- Plan docs or spikes

## Depends on

- Vue 3 peer/runtime
- D3 math packages listed in `package.json`

## Used by

- Package consumers importing from `vue-relative-rotation-chart`
- `demo/` during local development
