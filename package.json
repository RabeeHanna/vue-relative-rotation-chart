{
  "name": "vue-relative-rotation-chart",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vue SVG component for RRG-style relative rotation charts (renderer only).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "spike:labels": "vitest run spikes/label-collision"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
